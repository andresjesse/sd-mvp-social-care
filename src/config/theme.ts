import { MantineThemeOverride } from "@mantine/core";

const theme: MantineThemeOverride = {
  colorScheme: "light",
  primaryColor: "teal",
  defaultRadius: "sm",
  fontFamily: "Roboto",
  primaryShade: 7,
};

export default theme;
