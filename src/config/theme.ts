import { MantineThemeOverride } from "@mantine/core";

const theme: MantineThemeOverride = {
  colorScheme: "light",
  primaryColor: "teal",
  defaultRadius: 8,
  fontFamily: "Roboto",
  // primaryShade: 7,
};

export default theme;
